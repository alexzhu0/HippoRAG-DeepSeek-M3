[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
markers = ["mps: requires a physical or self-hosted Mac with a working Metal GPU"]

[tool.ruff]
target-version = "py310"
line-length = 110

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]
